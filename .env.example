# 在项目根目录复制为 .env.local，再填写自己的 cpolar HTTPS 根地址。
# Vite 使用其中的主机名设置白名单；Android TV 将其作为正式版的默认服务器地址。
# 直接填写完整地址，不要包含 /display 路径、账号密码、查询参数或变量引用。
# 读取优先级：系统环境变量 > .env.local > .env。修改后重启 Vite，并重新构建 APK。
POKER_SERVER_URL=https://poker.example.com

# 网页分享元数据使用的站点根地址，可以填写自己的 Sites 域名或自定义域名。
# 用于生成 /og.png 等资源的绝对地址；无需与电视服务器地址相同。
# 未配置时回退为 http://localhost:3000。修改后需重新构建并发布网站。
# 此地址会出现在页面元数据中，仅用于避免在公开源码中硬编码私人域名。
POKER_SITE_URL=https://poker.example.com
